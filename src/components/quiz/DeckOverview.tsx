/**
 * @deprecated DELETED IN STAGE 1 (2026-05-01). This file is a stub awaiting
 * physical removal — the sandbox could not `rm` it, so the contents were
 * neutralised and Stage 9 of `_local/quiz_overhaul_plan.md` lists it for
 * `git rm`. Do not import. The new pill row in Stage 4 covers the same use
 * case (jump-to-question + answered-state visibility).
 */
export {};
