/**
 * DEPRECATED — replaced by SourcesStripsView.tsx (Streifen idiom for the
 * Informationsquellen tab). The legacy V2 view (dumbbell / small multiples /
 * strategy matrix) was retired together with V1 in favour of a single
 * Streifen-style tab. Kept as an empty stub only because file deletion is not
 * available in the current environment; the component is no longer imported
 * anywhere. Safe to delete this file when convenient.
 */
export default function InformationSourcesV2View() {
  return null;
}
