/**
 * DEPRECATED — replaced by SourcesStripsView.tsx (Streifen idiom for the
 * Informationsquellen tab). Kept as an empty stub only because file deletion
 * is not available in the current environment; the component is no longer
 * imported anywhere. Safe to delete this file when convenient.
 */
export default function InformationSourcesView() {
  return null;
}
