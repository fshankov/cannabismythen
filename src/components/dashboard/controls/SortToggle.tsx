/**
 * @deprecated Stage 6 of the Daten-Explorer refactor retired this component.
 * Sortierung now lives as a third <DataPicker> in the shared toolbar
 * (see MythenExplorer.tsx). The file is kept as a no-op stub because
 * file deletion requires user interaction in this environment — safe to
 * remove with `git rm` in a follow-up commit.
 */
export {};
