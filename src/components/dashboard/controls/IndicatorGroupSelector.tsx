/**
 * @deprecated Removed in Stage 2 of the Daten-Explorer refactor.
 *
 * The Balken / Tabelle indicator + group selector is now built from
 * the shared `<DataPicker>` primitive, rendered inside `<ToolbarRow>`
 * by `MythenExplorer.tsx`. This file is intentionally empty so any
 * stray import surfaces as a TypeScript error at build time. Delete
 * the file in a follow-up cleanup commit.
 */
export {};
