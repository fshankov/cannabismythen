---
export const prerender = true;

// Redirect to the canonical first-audience page so there's no duplicate
// /meine-interessen/ URL that renders Eltern inline and then immediately
// reloads when the user clicks the Eltern tab (which links to /eltern/).
return Astro.redirect("/meine-interessen/eltern/", 301);
---
